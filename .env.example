# Both values are PUBLIC (they ship in the browser bundle). Multiplayer uses only
# Supabase Realtime (Broadcast + Presence) — no tables, no auth, no storage.
# Any Supabase project works, including the free tier.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx_or_the_legacy_anon_jwt
